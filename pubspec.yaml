name: expense_tracker
description: "Ứng dụng quản lý chi tiêu cá nhân - Bài tập lớn"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Styling
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  
  # State Management
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  
  # Database
  sqflite: ^2.3.3+1
  path: ^1.9.0
  
  # Charts
  fl_chart: ^0.68.0
  
  # Navigation
  go_router: ^13.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  sqflite_common_ffi: ^2.3.2  # For testing
  mockito: ^5.4.4
  build_runner: ^2.4.12

flutter:
  uses-material-design: true
  